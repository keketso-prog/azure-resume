name: FINAL DEBUG TEST

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "STEP 1 WORKS"

      - name: Step 2
        run: echo "STEP 2 WORKS"

      - name: Step 3
        run: uname -a

      - name: Step 4
        run: ls -R
