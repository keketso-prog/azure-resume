name: Deploy Resume Debug

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Debug start
        run: echo "Workflow started successfully"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Show files
        run: ls -R
